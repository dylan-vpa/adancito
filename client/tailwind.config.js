/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./index.html",
        "./src/**/*.{js,ts,jsx,tsx}",
    ],
    theme: {
        extend: {
            colors: {
                // Integrating your CSS variables if needed, or relying on standard palette
            }
        },
    },
    plugins: [],
}
